esphome:
  name: kyleoffice
  platform: ESP8266
  board: d1_mini

wifi:
  ssid: "Ursa Major"
  password: "DIPPERdipper"
  
  manual_ip:
    static_ip: 10.0.3.1
    gateway: 10.0.0.1
    subnet: 255.0.0.0

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Kyleoffice Fallback Hotspot"
    password: "L)g1nesphome"

captive_portal:

# Enable logging
logger:

# Enable Home Assistant API
api:

ota:

sensor:
  - platform: dht
    model: DHT22
    pin: D1
    temperature:
      name: "Kyle Office Temperature"
    humidity:
      name: "Kyle Office Humidity"
    update_interval: 60s
